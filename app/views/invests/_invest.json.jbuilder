json.extract! invest, :id, :asset_name, :bought_amount, :buy_price, :buy_fees, :total_input, :sold_amount, :sell_price, :sell_fees, :total_output, :created_at, :updated_at
json.url invest_url(invest, format: :json)
